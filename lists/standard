# /usr/share/live/build/lists/standard - package list for live-build(7)

#include <minimal>

console-setup console-common kbd


#if MODE emdebian debian ubuntu
locales
#endif

#if MODE progress
locales-all
#endif

#if MODE ubuntu
ubuntu-minimal
#endif
